{
  "name": "agenteco-buyer-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Demo Buyer Agent (\"Agent D\") — a standalone process built on the shared agent-runtime. Deploys independently from backend/frontend (its own Railway service).",
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file=.env src/index.ts",
    "start": "tsx --env-file=.env src/index.ts",
    "typecheck": "tsc --noEmit -p ."
  },
  "dependencies": {
    "viem": "^2.56.8"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4",
    "typescript": "^5"
  }
}
