/** Fixed windows applied to every escrow created through the MVP UI. */
export const EXECUTION_WINDOW_SECONDS = BigInt(24 * 60 * 60) // 24h
export const REVIEW_WINDOW_SECONDS = BigInt(48 * 60 * 60) // 48h
